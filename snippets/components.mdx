export const HeroCard = ({ img, title, description, href }) => {
  return (
    <a className="pb-8 cursor-pointer" href={href}>
      <img src={img} alt={title} className="w-full" />
      <h2 className="mt-4 font-semibold text-gray-900 dark:text-white">{title}</h2>
      <p className="mt-1 text-gray-600 dark:text-gray-400 text-sm leading-6">{description}</p>
    </a>
  );
};

export const Banner = () => {
  return (
    <div className="flex not-prose bg-primary-dark" style={{ borderRadius: "1rem" }}>
      <div className="flex-1" style={{ padding: "2rem 2rem" }}>
        <h2 className="text-3xl text-white font-bold">A Quickstart Guide</h2>
        <p className="mt-1 text-white">The first steps to help you get started with the CentML Platform.</p>
        <a href="/home/quickstart" className="mt-5 space-x-1 !text-black text-sm leading-6 font-medium px-4 py-1 bg-white rounded-xl hover:opacity-[0.9]">
          Get Started
        </a>
      </div>
    </div>
  );
};
