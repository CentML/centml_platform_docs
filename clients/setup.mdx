---
title: 'Client Setup'
description: 'Installation steps for the NVIDIA CCluster client'
icon: 'square-terminal'
---

We provide a client library to programmatically interact with NVIDIA CCluster either through [command line](/resources/cli) or using [Python SDK](/resources/sdk).

Use the following command to install centml client.

```bash
pip install centml
```

Run the following command to log in using your NVIDIA CCluster client credentials.

```bash
centml login
```

The first time, you'll be prompted to log in to your CCluster account through a browser. Once logged in, you are all set to use the client. You can use the `centml` CLI command to list your deployments.

```bash
centml cluster ls

# Alternatively, as a shorthand
ccluster ls
```

Checkout other supported commands,
```bash
ccluster --help
Usage: ccluster [OPTIONS] COMMAND [ARGS]...

  NVIDIA CCluster CLI tool

Options:
  --help  Show this message and exit.

Commands:
  delete  Delete a deployment
  get     Get deployment details
  ls      List all deployments
  pause   Pause a deployment
  resume  Resume a deployment
```

To logout, use the following command.

```bash
centml logout
```


## What's next

<CardGroup cols={2}>
  <Card
    title="Managing Vault Objects"
    icon="key-skeleton-left-right"
    href="/resources/vault"
  >
    Learn how to generate and store NVIDIA CCluster tokens and other vault objects.
  </Card>
  <Card
    title="Clients"
    icon="terminal"
    href="/clients/setup"
  >
    Learn how to interact with the NVIDIA CCluster programmatically
  </Card>
    <Card
    title="Get Support"
    icon="headset"
    href="/resources/requesting_support"
  >
    Submit a Support Request
 </Card>
  <Card
    title="Python SDK Reference"
    icon="python"
    href="/clients/sdk"
  >
    Learn how to access the NVIDIA CCluster using Python.
  </Card>
  <Card
    title="The Model Integration Lifecycle"
    icon="arrows-spin"
    href="/resources/model_integration_lifecycle"
  >
    Dive into how NVIDIA CCluster can help optimize your Model Integration Lifecycle (MILC).
 </Card>
</CardGroup>
